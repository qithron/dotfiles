test -d "$RUSTUP_HOME" && export MANPATH="$RUSTUP_HOME/toolchains/stable-x86_64-unknown-linux-gnu/share/man:$MANPATH"
cmd-exists rustup && export RUSTUP_HOME="$XDG_DATA_HOME/rustup"
cmd-exists cargo && export CARGO_HOME="$XDG_DATA_HOME/cargo"
