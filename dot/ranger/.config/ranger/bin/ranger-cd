#!/usr/bin/env sh

# cd to ranger last exit dir
#
# for usage see "e" function in:
#   ~/.bashrc
#   ~/.config/fish/config.fish

if [ "$RANGER_TMP" ]; then
    f="$RANGER_TMP/ranger-last-dir-ranger"
    test -f "$f" && cat "$f" && rm "$f"
fi
